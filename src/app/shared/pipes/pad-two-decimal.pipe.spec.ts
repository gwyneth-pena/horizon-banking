import { PadTwoDecimalPipe } from './pad-two-decimal.pipe';

describe('PadTwoDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new PadTwoDecimalPipe();
    expect(pipe).toBeTruthy();
  });
  
});
